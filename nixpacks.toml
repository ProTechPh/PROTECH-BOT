[phases.setup]
nixPkgs = ["python313", "docker", "cloudflared"]

[phases.install]
cmds = ["python -m venv .venv", ". .venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "python bot.py"
